# Taibai MCP Server Configuration

# Directory where Dedao content will be downloaded
# Defaults to current directory if not set
DEDAO_DOWNLOAD_DIR=/path/to/your/obsidian/vault/Dedao/Courses

# Optional: Custom path to dedao-dl binary (if not in PATH)
# DEDAO_DL_PATH=/usr/local/bin/dedao-dl

# Optional: Custom working directory for dedao-dl
# Defaults to ~/.taibai
# TAIBAI_WORK_DIR=/home/user/.taibai